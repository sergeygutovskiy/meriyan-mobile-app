class Tour {
    int id;
    String title;
    String text;
    int price;
    String imagePath;

    Tour({
        required this.id,
        required this.title,
        required this.text,
        required this.price,
        required this.imagePath
    });

}